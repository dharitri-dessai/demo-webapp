{% extends 'base.html.twig' %}

{% block body %}
    <h1>Welcome</h1>
    {% if app.user %}
        <div class="mb-3">
             <a href="{{path('product_index') }}"> Products </a>
        </div>
    {% else %}
        <div class="mb-3">
             <a href="{{path('app_register') }}"> Register </a> <br/> <a href="{{ path('app_login') }}">Login</a>
        </div>
    {% endif %}
{% endblock %}